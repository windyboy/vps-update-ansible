---
# Debian/Ubuntu specific variables

# Package manager
package_manager: apt

# Update cache command
update_cache_timeout: 3600

# Packages to always exclude from updates (can be overridden)
default_excluded_packages: []
